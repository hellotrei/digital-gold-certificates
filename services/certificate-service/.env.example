PORT=4101
# Use a 32-byte hex private key (example: openssl rand -hex 32)
ISSUER_PRIVATE_KEY_HEX=<REPLACE_WITH_64_HEX_CHARS>
CERT_DB_PATH=./data/certificate-service.db
# Optional inter-service auth shared secret:
# SERVICE_AUTH_TOKEN=<REPLACE_WITH_SHARED_SECRET>
# Optional for proof anchoring:
# LEDGER_ADAPTER_URL=http://127.0.0.1:4103
